# Handoff — Fase 4 · Slice 3 · Sub-Slice S3.2.a (Hooks + Schemas + payment_type ES→EN) ✅ CERRADO

**Fecha:** 2026-05-24
**Estado del sub-slice:** ✅ Schemas extendidos + 7 consumers migrados ES→EN + 10 hooks nuevos creados + tsc/build verde sin regresiones + smoke SQL del CHECK constraint OK + commit local en `ux-fixes`
**Predecesor:** [`2026-05-24_PHASE-4-SLICE-3.1-BACKEND.md`](2026-05-24_PHASE-4-SLICE-3.1-BACKEND.md)
**Branch:** `ux-fixes` (donde vive S3.1 afa381f). El sufijo "slice-3-payment-flow" que sugería el refactor map quedó como branch nominal; toda la cadena vive físicamente en `ux-fixes`.

---

## TL;DR

S3.2.a (hooks + schemas) está **completo**:

- `paymentTypeSchema` migrado de ES (`anticipo/abono/pago_final/reembolso`) a EN (`advance/installment/final/refund`) — arregla el bug latente de S2 que rompía `NewPaymentModal` con PG 23514.
- 7 archivos consumers migrados (4 UI + 3 types/schemas).
- 10 hooks nuevos creados (4 finanzas + 3 quotations + 2 settings + 1 root).
- 0 errores tsc nuevos (34 baseline = 34 post-cambios).
- Build pasa en 14.78s.
- Smoke SQL: 4 INSERTs con cada valor inglés `payment_type` aceptados por el CHECK; cleanup 100%; 0 side effects.

La próxima sesión arranca **S3.2.b** (UI admin: páginas Settings + modales Verify/ManualPayment/Cancel + refactor `Pagos.tsx` con 3 tabs + `QuotationDetail` botones).

---

## Cambios aplicados

### Schemas (M-6, M-7 del refactor map)

| Archivo | Cambio |
|---|---|
| [src/schemas/payment.ts](../../src/schemas/payment.ts) | `paymentTypeSchema` migrado a inglés. Agregados `paymentSourceSchema`, `verificationStatusSchema`, `clientFacingPaymentMethodSchema`, `submitPaymentProofSchema`, `verifyPaymentSchema`, `rejectPaymentSchema`, `registerManualPaymentSchema`. Mapas de display: `PAYMENT_TYPE_LABELS_ES`, `PAYMENT_METHOD_LABELS_ES`, `VERIFICATION_STATUS_LABELS_ES`, `PAYMENT_SOURCE_LABELS_ES`. |
| [src/schemas/quotation.ts](../../src/schemas/quotation.ts) | `quotationStatusSchema` extendido con `client_approved`, `pending_payment_verification`, `cancelled`, `superseded` (estados que prod ya tenía pero el schema desconocía). Mapa `QUOTATION_STATUS_LABELS_ES`. Schemas nuevos: `cancelQuotationAcceptanceSchema`, `createQuotationRevisionSchema`, `reactivateExpiredQuotationSchema`. |

> ⚠️ Nota: `src/schemas/quotation.schema.ts` (el del engine de cotizadores) NO se tocó — sigue con el enum viejo (8 valores) porque el motor de cálculo no necesita conocer los estados de lifecycle de S3. El barrel `src/schemas/index.ts` ya re-exporta solo `./quotation`, no `./quotation.schema`.

### Types (deuda histórica corregida)

| Archivo | Cambio |
|---|---|
| [src/types/database.ts](../../src/types/database.ts) (línea 149) | `payment_type` union de `'anticipo'|'abono'|'pago_final'|'reembolso'|string` → `'advance'|'installment'|'final'|'refund'|string`. |
| [src/types/database.types.ts](../../src/types/database.types.ts) (líneas 2471, 2653) | Enum huérfano `Database["public"]["Enums"]["payment_type"]` migrado a inglés. El column type real seguía siendo `string \| null` así que esto es cosmético — pero deja el archivo consistente con la realidad del CHECK. |

### Consumers UI migrados a inglés (parte de M-8)

| Archivo | Cambio |
|---|---|
| [src/components/finanzas/NewPaymentModal.tsx](../../src/components/finanzas/NewPaymentModal.tsx) | `TYPE_OPTIONS` y `METHOD_OPTIONS` ahora se derivan de `paymentTypeSchema.options` + `PAYMENT_TYPE_LABELS_ES`/`PAYMENT_METHOD_LABELS_ES`. No hay strings hardcoded. |
| [src/components/finanzas/PaymentDetailPanel.tsx](../../src/components/finanzas/PaymentDetailPanel.tsx) | Header status usa `PAYMENT_TYPE_LABELS_ES`. Display del método usa `PAYMENT_METHOD_LABELS_ES`. |
| [src/components/finanzas/PaymentsList.tsx](../../src/components/finanzas/PaymentsList.tsx) | `getTypeBadge` switch reescrito con keys EN + labels desde el mapa. |
| [src/pages/Pagos.tsx](../../src/pages/Pagos.tsx) | FilterSheet de `payment_method` y `payment_type` ahora renderizan `<SelectItem>` desde `paymentMethodSchema.options` + `paymentTypeSchema.options` con labels del mapa. |

### Dashboard / usePayments

NO requieren cambio:
- `Dashboard.tsx` solo pasa `payment_type: "all"` (passthrough sin comparar valor).
- `usePayments.ts` solo hace `.eq("payment_type", filters.payment_type)` (string opaco).

### Hooks nuevos (N-8 a N-17 del refactor map)

| # | Path | Responsabilidad |
|---|---|---|
| N-8 | [src/hooks/finanzas/useSubmitPaymentProof.ts](../../src/hooks/finanzas/useSubmitPaymentProof.ts) | **Cliente público.** Upload a `payment-receipts/<quotation_id>/<uuid>.<ext>` (5MB max, image/* + pdf) + RPC `submit_quotation_payment_proof`. Compensa con `remove([path])` si la RPC falla post-upload. Validación size/mime client-side antes de tocar storage. |
| N-9 | [src/hooks/finanzas/useVerifyPayment.ts](../../src/hooks/finanzas/useVerifyPayment.ts) | **Admin.** RPC `verify_payment(payment_id, designer_id, payment_type?)`. Invalida `['payments', 'projects', 'quotations', 'opportunities', 'notifications']`. |
| N-10 | [src/hooks/finanzas/useRejectPayment.ts](../../src/hooks/finanzas/useRejectPayment.ts) | **Admin.** RPC `reject_payment(payment_id, reason)`. Invalida `['payments', 'quotations', 'notifications']`. |
| N-11 | [src/hooks/finanzas/useRegisterManualPayment.ts](../../src/hooks/finanzas/useRegisterManualPayment.ts) | **Admin.** RPC `register_manual_payment(...)`. Invalida igual que N-9. |
| N-12 | [src/hooks/quotations/useCancelQuotationAcceptance.ts](../../src/hooks/quotations/useCancelQuotationAcceptance.ts) | **Admin.** RPC `cancel_quotation_acceptance(quotation_id, reason)`. Invalida `['quotation', id]`, `['quotations']`, `['opportunities']`, `['notifications']`. |
| N-13 | [src/hooks/quotations/useCreateQuotationRevision.ts](../../src/hooks/quotations/useCreateQuotationRevision.ts) | **Admin.** RPC `create_quotation_revision(quotation_id)`. Devuelve `new_quotation_id`. NO navega — el consumer decide. |
| N-14 | [src/hooks/quotations/useReactivateExpiredQuotation.ts](../../src/hooks/quotations/useReactivateExpiredQuotation.ts) | **Admin.** RPC `reactivate_expired_quotation(quotation_id)`. Invalida `['quotation', id]`, `['quotations']`, `['payments']`, `['notifications']`. |
| N-15 | [src/hooks/settings/useSystemSettings.ts](../../src/hooks/settings/useSystemSettings.ts) | `useSetting<T>(key)` (read JSONB) + `useUpdateSetting()` (upsert). queryKey `['system_settings', key]`. staleTime 60s. |
| N-16 | [src/hooks/settings/useFeatureFlag.ts](../../src/hooks/settings/useFeatureFlag.ts) | `useFeatureFlag(key): boolean`. Castea JSONB → bool aceptando `true`, `"true"`, `{enabled: true}`. Default false. staleTime 5min. |
| N-17 | [src/hooks/useActiveDesigners.ts](../../src/hooks/useActiveDesigners.ts) | `useActiveDesigners()` → `{id, full_name, whatsapp_phone}[]` con `role='diseno' AND is_active=true`. |

---

## Verificaciones realizadas

| Verificación | Comando | Resultado |
|---|---|---|
| Baseline tsc | `npx tsc --noEmit` | 34 errores (pre-existentes — `usePermissions`, `errors.ts`, `Materials`, `Tareas`, `UserCreate`, etc.) |
| tsc post-cambios | mismo | **34 errores** (idéntico al baseline; los 3 errores de Pagos.tsx solo cambiaron de línea por shift de imports). Cero regresiones nuevas. |
| Build | `npm run build` | ✅ 14.78s. `Pagos-HYNrABx6.js` 30.79kB. Solo warnings de chunk size pre-existentes (`chunk-pdf 594kB`). |
| Smoke CHECK constraint | `pg_get_constraintdef` via Management API | `CHECK (payment_type = ANY (ARRAY['advance', 'installment', 'final', 'refund']))` — confirmado inglés. |
| Smoke INSERT por type | 4× `INSERT INTO payments` con cada valor inglés sobre `[QA-SLICE2-2026-05-23]` client | ✅ todas aceptadas. 0 errors 23514. 0 side effects (sin projects creados, sin WA encolados — los payments tenían `quotation_id IS NULL`). Cleanup 4/4 DELETE OK. |

**Smoke pendiente (no bloqueante para S3.2.b)**: prueba visual del `NewPaymentModal` en navegador real (login admin → fill form → submit). El cambio es mecánico (Spanish→English en el payload) y el CHECK acepta los 4 valores via SQL directo, por lo que la confianza del cambio es alta sin la prueba visual. Si el usuario quiere validarlo: `npm run build && npm run preview` y crear un pago con cada type.

---

## Archivos NO tocados (deuda postergada)

Documentado por completitud para que la próxima IA no se distraiga:

- `lib/queryKeys.ts` inexistente — keys ad-hoc. Out of scope S3.
- `db/supabase_schema.sql` desactualizado vs prod. Out of scope S3.
- `src/types/database.types.ts` es generado y va a contradecirse en el próximo `supabase gen types`. Cuando se regenere, validar que sigue alineado con el CHECK actual.
- `src/schemas/quotation.schema.ts` con enum viejo de 8 valores — solo el engine de cotizadores lo consume; sin riesgo para S3 mientras NO se use para validar entity status.

---

## Para la siguiente sesión: S3.2.b (UI admin)

**Branch base**: continuar en `ux-fixes` (donde vive S3.1 + S3.2.a) o crear `slice-3-payment-flow` cortada de `ux-fixes`.

**Goal**: construir la UI de administración del flujo S3 — 2 páginas de settings + 3 modales + refactor de `Pagos.tsx` a 3 tabs + botones nuevos en `QuotationDetail`. Todo gated por `useFeatureFlag('slice_3_enabled')`.

### Prompt listo para copiar

```
Voy a continuar con S3.2.b (UI admin del flujo de pago Slice 3) de Innovar CRM. Los sub-slices anteriores están cerrados: ver `docs/handover/2026-05-24_PHASE-4-SLICE-3.2A-HOOKS-SCHEMAS.md` y `2026-05-24_PHASE-4-SLICE-3.1-BACKEND.md`.

ANTES DE TOCAR NADA, leé en este orden:

1. Handoff de cierre S3.2.a:
   `C:\Users\ceoel\OneDrive\Escritorio\mi proyect\Agents-automations\Innovar-App-main\docs\handover\2026-05-24_PHASE-4-SLICE-3.2A-HOOKS-SCHEMAS.md`

2. Refactor map autoritativo §2.1, §2.2, §3 (M-1 a M-5), §6 (cross-table invalidations), §8 (feature flag insertion points):
   `docs/architecture/slice-3-refactor-map.md`

3. Patrones existentes de la UI admin:
   - `src/pages/Settings.tsx` — landing con `QuickAccessGrid`. M-2 le agrega tercera sección "Operaciones Financieras".
   - `src/pages/settings/Holidays.tsx` y `Pricing.tsx` — patrón de página settings con form + persistencia. Base para N-1/N-2.
   - `src/pages/QuotationDetail.tsx` — donde van los botones nuevos M-5.
   - `src/pages/Pagos.tsx` (post S3.2.a) — base del refactor M-3 con tabs.
   - `src/components/finanzas/NewPaymentModal.tsx` (post S3.2.a) — patrón visual del modal nuevo.
   - `src/components/ui/tabs.tsx` (shadcn) — Component para las 3 tabs.

4. CLAUDE.md global + del proyecto.

---

ARRANCÁ S3.2.b:

### PR-3 — UI admin (8 archivos nuevos + 5 modificados)

**Nuevos**:
- `src/pages/settings/BankSettings.tsx` (N-1) — 7 inputs sobre `system_settings` con `useSystemSettings`.
- `src/pages/settings/PaymentSettings.tsx` (N-2) — toggle `slice_3_enabled` + `payment_window_days` + `suggested_min_advance_pct`.
- `src/components/finanzas/PaymentVerifyModal.tsx` (N-3) — modal único Verify/Reject con preview comprobante + `DesignerPicker` + dropdown `payment_type` + 2 botones. Llama `useVerifyPayment` / `useRejectPayment`.
- `src/components/finanzas/ManualPaymentModal.tsx` (N-4) — autocomplete cotización + form. Llama `useRegisterManualPayment`.
- `src/components/finanzas/DesignerPicker.tsx` (N-5) — `<Select>` con `useActiveDesigners()` + opción "asignar después".
- `src/components/quotations/QuotationCancelModal.tsx` (N-6) — textarea motivo + confirmar. Llama `useCancelQuotationAcceptance`.

**Modificados**:
- `src/App.tsx` (M-1): agregar rutas `/settings/bancarios` y `/settings/pagos` con role guards.
- `src/pages/Settings.tsx` (M-2): tercera sección "Operaciones Financieras" con 2 cards.
- `src/pages/Pagos.tsx` (M-3): refactor a 3 tabs (`Por verificar` / `Verificados` / `Rechazados`) + badge contador + botón "Registrar pago manual". TODO gated por `useFeatureFlag('slice_3_enabled')` — si OFF, render layout actual sin tabs ni botón manual. Para el tab "Por verificar", extender `usePayments` con filtro `verification_status` (1 línea en el hook).
- `src/pages/QuotationDetail.tsx` (M-5): botones "Cancelar aceptación" + "Crear versión nueva" + "Reactivar" condicionados por status y flag.

#### Reglas de implementación
- Flag default OFF → cada surface tiene fallback al UI legacy. Cero impacto en prod hasta que admin toggle.
- Cero hardcoded de payment_type/method strings — usar `PAYMENT_TYPE_LABELS_ES`/`PAYMENT_METHOD_LABELS_ES` + `paymentTypeSchema.options`/`paymentMethodSchema.options`.
- Para componer queryKeys, mantener convención: `['payments']` (todas las variantes), `['quotation', id]`, etc.
- NUEVA prop para `usePayments`: `verification_status?: string`. Implementar como filtro adicional `query.eq("verification_status", v)` cuando se pasa.
- Si necesitás extender NewPaymentModal para que también acepte registro manual S3, mejor crear ManualPaymentModal por separado y dejar NewPaymentModal intacto (es legacy del flujo de S2 sin quotation/proyecto).

#### Verificar
- `tsc --noEmit`: 0 errores nuevos vs baseline 34.
- `npm run build`: OK.
- `npm run preview` + login admin → ir a `/settings` → ver 3a sección → click "Datos Bancarios" → 7 inputs vacíos cargan → click "Configuración de Pagos" → toggle + 2 inputs cargan (flag OFF). Toggle ON manual via PaymentSettings → ir a `/pagos` → ver 3 tabs + botón "Pago manual". Click tab "Por verificar" → contador en 0 (no hay pending S3 todavía). Toggle OFF → tabs desaparecen.
- NO `npm run dev` (OneDrive HMR rompe).

#### Commit local (no push)
Mensaje: `feat(slice-3): UI admin (settings + modales + tabs Pagos + QuotationDetail buttons)`.

#### Handoff a S3.3
Escribir `docs/handover/2026-05-XX_PHASE-4-SLICE-3.2B-UI-ADMIN.md` con paso a paso visual + screenshots si querés. Prompt para S3.3.

NO arranques S3.3 en la misma sesión.

---

Reglas operativas no negociables:
- DB en inglés (`advance/installment/final/refund`), UI en español (mapa labels).
- MCP nativo NO tiene scope a Innovar — usar Management API + `SUPABASE_ACCESS_TOKEN` del `.env` + curl + Node.
- NUNCA `npm run dev` (OneDrive rompe HMR). Usar `npm run build && npm run preview`.
- NUNCA SDK Supabase desde `onAuthStateChange`.
- NUNCA `enqueue_notification` para in-app — INSERT directo a `public.notifications`.
- Realtime channel hardcoded = singleton crash; reusar `notifications-updates` del Layout.
- Commits locales los hacés vos. NUNCA `git push` salvo pedido explícito. NUNCA `vercel --prod`.

URLs:
- Supabase Innovar: https://supabase.com/dashboard/project/xdzbjptozeqcbnaqhtye
- Vercel: https://crm-innovar-app-2026.vercel.app
- Repo: https://github.com/accesos-seo/innovar-crm

Cuando termines de leer, decime "Listo, arranco S3.2.b" y seguís.
```

---

## Templates Meta WhatsApp pendientes (sin cambios)

Sin cambios respecto a S3.1: 9 templates pendientes (4 S2 + 5 S3 nuevos). Bloqueador externo de Felipe.

---

## Confianza del handoff

- **Schemas + types**: alta. Validado con tsc post-cambios (sin nuevos errores).
- **Consumers migrados**: alta. Inspeccionados los 5 archivos, eliminados todos los string literales en español. Display siempre derivado de `PAYMENT_TYPE_LABELS_ES`.
- **Hooks**: alta para los 10 archivos. Cada uno sigue el patrón de los hooks existentes en `src/hooks/quotations/` (mutation con `mapSupabaseError` + invalidaciones explícitas + toast). Las invalidaciones cross-table están alineadas con refactor map §6.
- **Smoke CHECK constraint**: alta. SQL directo contra prod aceptó los 4 valores inglés. Cero side effects.
- **Pendiente sin riesgo conocido**: prueba visual del `NewPaymentModal` (creación E2E desde UI). El cambio es mecánico ya validado por tsc/build/SQL.
