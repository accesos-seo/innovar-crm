# Handoff — Innovar CRM
**Fecha:** 2026-06-09  
**Rama:** `ux-fixes` (pusheada a `origin/ux-fixes`)  
**Último commit:** `185697b`

---

## Context

Innovar CRM es un CRM a medida para Innovar Cocinas de Diseño (Pereira, Colombia). Stack: React + Supabase (`xdzbjptozeqcbnaqhtye`) + Edge Functions + Meta WhatsApp Business API + n8n. Tiene un Motor Comercial de 9 etapas (Captura → Producción) con notificaciones WA automáticas en cada transición.

---

## Current state

### ✅ Smoke test E2E completado — rama `ux-fixes`

Se hizo un smoke test completo del Motor Comercial. Se encontraron y corrigieron **5 bugs silenciosos** en el sistema de notificaciones WA. Todo el flujo de templates está operativo excepto un pending de Meta.

**Commits de esta sesión:**
| Commit | Descripción |
|---|---|
| `e2de3a9` | fix: bienvenidas_clientes → welcome_lead_v1 + mover JWT a system_settings |
| `c4b9f16` | fix(042): guard NULL en anon_key + COALESCE redundante eliminado |
| `598fd3f` | fix(042): DECLARE anidado → variables al DECLARE principal |
| `8cd984a` | fix: appointment_booked + task_assigned al TEMPLATE_REGISTRY |
| `185697b` | docs: handoff smoke test Motor Comercial |

**Todos los cambios pusheados a `origin/ux-fixes`.**

---

### Bugs corregidos (ya en producción)

| # | Template | Bug | Fix |
|---|---|---|---|
| 1 | `quotation_sent_v1` | Faltaba en TEMPLATE_REGISTRY — WA de cotización nunca llegaba | Agregado + EF deploada |
| 2 | `notify_lead_followup_flow` | Usaba `bienvenidas_clientes` (nombre antiguo) | Reescrita con `welcome_lead_v1` + guard NULL para anon_key |
| 3 | `send_quotation_to_client` | No asignaba `valid_until` al enviar | Corregido: `CURRENT_DATE + quotation_validity_days` (default 30d) |
| 4 | `appointment_booked` | Faltaba en TEMPLATE_REGISTRY | Agregado + EF deploada |
| 5 | `task_assigned` | Faltaba en TEMPLATE_REGISTRY | Agregado + EF deploada |

**Migración:** `db/migrations/042_fix_welcome_lead_and_quotation_valid_until.sql` (comprometida y aplicada en prod ✅)

---

### 🔴 Único pendiente — acción de Robert en Meta BM

**`visit_assigned_admin_v1`** → Meta error `#132001`: el template no existe en la traducción de Meta Business Manager.

**Acción:**
1. Ir a [Meta Business Manager → Message Templates](https://business.facebook.com/wa/manage/message-templates/) con cuenta de Innovar Cocinas De Diseño
2. Buscar `visit_assigned_admin_v1` — si no existe, crear con 4 parámetros: `{{1}}` técnico, `{{2}}` cliente, `{{3}}` fecha/hora, `{{4}}` dirección
3. Esperar aprobación Meta (24–48h)
4. Una vez aprobado, la queue lo procesará automáticamente (cron cada 1 min)

---

## Estado de `notification_queue` al cierre

| Template | Status | Nota |
|---|---|---|
| `welcome_lead_v1` | processing (3) | Reseteadas de bienvenidas_clientes/failed |
| `appointment_booked` | processing (1) | Reseteada de failed |
| `task_assigned` | processing (1) | Reseteada de failed |
| `booking_link_v1` | sent (1) + processing (1) | OK |
| `admin_quotation_expired_v1` | sent (1) | OK |
| `visit_assigned_admin_v1` | **failed (1)** | 🔴 Meta #132001 — ver pendiente |

---

## Next steps

### 1. Activar en producción real (cuando Robert valide los mensajes)

Cambiar `wa_test_phone_override` para dejar de redirigir WA a Robert:

```sql
UPDATE public.system_settings
SET value = 'null'::jsonb
WHERE key = 'wa_test_phone_override';
```

Supabase proyecto: `xdzbjptozeqcbnaqhtye` — ejecutar vía Management API o dashboard.

### 2. Resolver `visit_assigned_admin_v1` en Meta BM

Ver sección "único pendiente" arriba.

### 3. Merge de `ux-fixes` a `main`

Cuando Robert confirme que todo funciona en producción:

```bash
cd D:\Agents-automations\04-Innovar
git checkout main
git merge ux-fixes
git push origin main
```

### 4. Verificar `task_assigned` en Meta BM

El template `task_assigned` está marcado como "en registro" en el catálogo. Confirmar que está aprobado en Meta BM con ese nombre exacto (sin `_v1`). Ver `reference_innovar_whatsapp_templates.md` en Engram.

### 5. Deploy Vercel (cuando sea necesario)

Solo si se hacen cambios de UI. El proyecto en Vercel es `crm-innovar-app-2026.vercel.app`. Credencial en `.env` como `VERCEL_TOKEN`.

---

## Key references

| Item | Valor |
|---|---|
| **Repo** | `D:\Agents-automations\04-Innovar` |
| **Rama activa** | `ux-fixes` (pusheada) |
| **Supabase proyecto** | `xdzbjptozeqcbnaqhtye` |
| **Vercel URL** | `https://crm-innovar-app-2026.vercel.app` |
| **wa_test_phone_override** | `3183061286` (Robert) — aún activo |
| **slice_3_enabled** | `true` |
| **Edge Function** | `process-whatsapp-notifications` (deploada `8cd984a`) |
| **Migración principal** | `db/migrations/042_fix_welcome_lead_and_quotation_valid_until.sql` |
| **PAT** | `SUPABASE_ACCESS_TOKEN` en `.env` local |
| **Templates WA** | `C:\Users\ceoel\.claude\projects\C--Users-ceoel\memory\reference_innovar_whatsapp_templates.md` |
| **Catálogo WA completo** | `docs/handover/2026-06-09_smoke-test-motor-comercial.md` |

## Suggested skills

- `/diagnose` — si algún template WA sigue fallando después de que Robert apruebe en Meta
- `/deploy-check` — antes de merge a main
