# Handoff — Innovar CRM · Fase 4 · Slice 1 (Cimientos DB) · CERRADO

> **Fecha**: 2026-05-23 · **Branch**: `master` · **Working dir**: `C:\Users\ceoel\OneDrive\Escritorio\mi proyect\Agents-automations\Innovar-App-main`
> **Plan global de Fase 4**: [`docs/handover/2026-05-23_PHASE-4-PLAN-LOCKED.md`](2026-05-23_PHASE-4-PLAN-LOCKED.md)

---

## 0. TL;DR

Slice 1 completado: **4 migraciones aplicadas a producción + 1 usuario nuevo en `auth.users`**. Cero código frontend. Cero feature flag activada todavía. Soak de 24h antes de arrancar Slice 2.

---

## 1. Qué se aplicó

| Migración | Estado | Qué hace |
|---|---|---|
| `030_phase4_quotations_new_columns.sql` | ✅ | 7 columnas nuevas en `quotations` (notas/razones/subtipo/timestamps/tracking) + `clients.first_project_at` + CHECK en subtype |
| `031_phase4_bank_settings_seeds.sql` | ✅ | 7 filas vacías en `system_settings` (bank_*, nequi_phone, daviplata_phone) + helper `get_bank_setting(key)` |
| `032_phase4_designer_qa_seed.sql` | ✅ | Profile "Diseñador QA" con UUID `d1591a8e-2026-4f4d-9d11-1100519d5ccc`, role `diseno`, WhatsApp `+573183061286` (confirmado por Alvaro) + helper `get_active_designers()` |
| `033_phase4_storage_buckets.sql` | ✅ | Bucket `payment-receipts` (5MB, image/pdf) + `quotation-pdfs` (10MB, pdf), ambos privados, con RLS estricta |

**Acción manual ejecutada antes de 032**: `POST https://xdzbjptozeqcbnaqhtye.supabase.co/auth/v1/admin/users` con UUID estable + email `diseno.test@innovar.local` + password `InnovarTest2026!DisenoQA`. Respuesta 200 con `email_confirmed_at` seteado.

---

## 2. Smoke tests corridos (todos verde)

```
quotation_columns      → 7 filas: client_acceptance_note, client_approved_at,
                         client_rejected_at, client_rejection_reason,
                         client_rejection_subtype, view_count, viewed_at
client_column          → first_project_at presente
bank_settings          → 7 keys: bank_account_number, bank_account_type,
                         bank_holder_id, bank_holder_name, bank_name,
                         daviplata_phone, nequi_phone
designer_qa            → 1 row con UUID correcto, role=diseno, is_active=true,
                         whatsapp_phone=+573183061286
buckets                → payment-receipts (5MB, public=false) + quotation-pdfs
                         (10MB, public=false)
helpers                → get_active_designers + get_bank_setting presentes
rejection_check        → constraint quotations_client_rejection_subtype_check OK

quot_select            → SELECT * FROM quotations devuelve 11 filas (no rompe)
cli_select             → SELECT * FROM clients devuelve 30 filas (no rompe)
designers_helper       → get_active_designers() devuelve 1 row (el QA)
bank_helper_empty      → get_bank_setting('bank_name') devuelve '' (vacío)
```

---

## 3. Qué falta verificar (acción tuya, Alvaro)

Antes de arrancar Slice 2:

- [ ] Abrir la app en producción y navegar `/cotizaciones`, `/clientes` — confirmar que cargan sin errores en consola. Si rompen, el problema NO está en las columnas nuevas (ninguna query del front las usa todavía); reportar igual.
- [ ] Idealmente dejar 24h de soak antes de aplicar el siguiente bloque (paranoid baseline para detectar regresiones de schema en queries que no anticipamos).
- [ ] **Templates Meta para Slice 2**: si todavía no se solicitaron a Felipe, abrir los 2 que bloquean S2:
  - `quotation_sent_v1` (variables: nombre, número cotización, link)
  - `quotation_v2_sent_v1` (variables: nombre, número cotización, link)
  - Wording exacto en [`docs/prd/phase-4-quotation-approval.md`](../prd/phase-4-quotation-approval.md) §4.4.

---

## 4. Cómo verificar en el dashboard de Supabase (opcional, para tranquilidad visual)

1. **Tabla `quotations`**: dashboard → Table editor → quotations → confirmar las 7 columnas nuevas.
2. **Tabla `system_settings`**: dashboard → Table editor → system_settings → filtrar por `key` empezando con `bank_` o `nequi_` o `daviplata_` → ver las 7 filas con `value = ""` (string vacío).
3. **Profiles**: dashboard → Authentication → Users → buscar `diseno.test@innovar.local` → ver el usuario activo.
4. **Storage**: dashboard → Storage → confirmar que existen los buckets `payment-receipts` y `quotation-pdfs`, ambos marcados como privados (icono candado).

---

## 5. Próximo paso — Slice 2

**No avanzar hasta que confirmes que Slice 1 está sólido en producción (24h sugerido).**

Slice 2 agrega:
- Migraciones `034` (4 RPCs públicas) + `035` (lock trigger + sync opp + accept/reject triggers + unlock RPC + send RPC + audit).
- Frontend: página `PublicQuotation` en `/cotizacion/:token`, botones de acción, vista de versión histórica/expirada, badge de bloqueo, botón "Enviar al cliente" en `QuotationDetail`.
- Bloqueador externo: aprobación Meta de 2 templates (mencionados arriba).

**Comando para arrancar Slice 2** (cuando estés listo):

```
"Arranquemos Slice 2 de Fase 4"
```

La próxima sesión leerá este handoff + el plan global y aplicará las migraciones 034-035 antes de tocar frontend.

---

## 6. Comando git para vos (copiar y pegar en tu PowerShell)

> Race condition con OneDrive: el agente no corre commits acá. Estos archivos cambiaron en disco — no hay commits pendientes míos, los que cambiaron son los que vos vas a stagear.

```powershell
cd "C:\Users\ceoel\OneDrive\Escritorio\mi proyect\Agents-automations\Innovar-App-main"

git add db/migrations/032_phase4_designer_qa_seed.sql db/migrations/README.md docs/handover/2026-05-23_PHASE-4-SLICE-1.md

git commit -m "feat(phase-4-s1): aplicar cimientos DB (030-033) + handoff Slice 1

Migraciones aplicadas via Management API:
- 030: 7 columnas nuevas en quotations + clients.first_project_at + CHECK
- 031: 7 filas bank_* en system_settings + helper get_bank_setting()
- 032: profile disenador QA (UUID estable, role=diseno, WhatsApp Alvaro)
       + helper get_active_designers()
- 033: buckets payment-receipts (5MB) + quotation-pdfs (10MB) con RLS

Pre-req ejecutado: auth.user diseno.test@innovar.local creado via
service_role API (UUID d1591a8e-2026-4f4d-9d11-1100519d5ccc).

Smoke tests verdes: queries existentes intactas, helpers devuelven data
esperada, CHECK constraint validada.

Next: 24h soak -> Slice 2 (URL publica + accept/reject/lock + sync opp).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"

# (no push hasta confirmar Slice 2 listo, o pushea ahora si querés)
# git push origin master
```

---

## 7. Memoria global actualizada

Entry en MEMORY.md global cambiada a:

> 🟢 **[Innovar — Fase 4 Slice 1 CERRADO 2026-05-23](project_innovar.md)** — 4 migraciones DB aplicadas (030-033) + auth.user diseñador QA creado. Cero frontend. Smoke OK. Próximo: 24h soak → Slice 2 (URL pública + acceptance/rejection + lock + sync). Bloqueador Slice 2: Meta aprueba `quotation_sent_v1` + `quotation_v2_sent_v1`.
